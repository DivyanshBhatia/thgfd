# TH-GFD Dependencies
# 
# NOTE: PyTorch Geometric 2.4+ includes scatter/sparse operations natively!
# You no longer need to install torch-scatter and torch-sparse separately.
#
# Installation:
#   pip install torch>=2.0.0
#   pip install torch-geometric>=2.5.0  # Uses native PyTorch operations
#   pip install -r requirements.txt
#
# Or for older PyG versions that need torch-scatter/torch-sparse:
#   pip install torch==2.2.0  # Use a version with available PyG wheels
#   pip install torch-scatter torch-sparse -f https://data.pyg.org/whl/torch-2.2.0+cpu.html

# Core PyTorch
torch>=2.0.0

# PyTorch Geometric (2.5+ uses native scatter/sparse, no separate packages needed)
torch-geometric>=2.5.0
# Note: torch-scatter and torch-sparse are NO LONGER required for PyG 2.4+
# The operations are now handled natively by PyTorch

# Data processing
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Experiment tracking
tqdm>=4.65.0
wandb>=0.15.0  # optional

# Baselines
xgboost>=1.7.0
lightgbm>=4.0.0

# Utilities
pyyaml>=6.0
